query get_users {
  description "Get all users from database"

  input {
    limit: number = 10
    offset: number = 0
  }

  stack {
    $users = db.query("users")
      .limit($input.limit)
      .offset($input.offset)
  }

  response {
    users: $users
  }
}
