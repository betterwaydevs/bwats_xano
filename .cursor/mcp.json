{
  "mcpServers": {
    "xano": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-remote",
        "https://xano.atlanticsoft.co/x2/mcp/meta/mcp/sse",
        "--header",
        "Authorization:${AUTH_TOKEN}"
      ],
      "env": {
        "AUTH_TOKEN": "Bearer ${XANO_TOKEN}"
      }
    }
  }
}
